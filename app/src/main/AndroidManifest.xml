<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myaccfinalapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/my_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmployerprofileActivity"></activity>
        <activity android:name=".MaidhomeActivity" />
        <activity android:name=".EmployerhomeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MaidregistrationActivity" />
        <activity android:name=".EmployerregistrationActivity" />
        <activity android:name=".EmployerloginActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>